import { sanitizeFilename, validateUpload, sha256Hex, deriveContradictions, setContradictionResolution, calculateReadiness, isManualFact, serializeIncident, restoreIncident } from './core.js';

const KEY = 'cybersutra.mock.case.v2';
const STEPS = ['Describe', 'Evidence', 'Timeline', 'Review', 'Readiness', 'Report', 'Acknowledgement'];
const $ = (selector, root = document) => root.querySelector(selector);
const id = prefix => `${prefix}_${crypto.randomUUID ? crypto.randomUUID() : Math.random().toString(36).slice(2)}`;
const html = value => String(value ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#39;');
const shortHash = hash => hash ? `${hash.slice(0, 6)}…${hash.slice(-4)}` : 'No file fingerprint available';
function newCase() { return { id: id('case'), description: '', evidence: [], facts: [], events: [], contradictions: [], ui: { selectedEvidenceId: null }, submitted: false, acknowledgement: null }; }
let state = load() || null;
function load() { try { const saved = localStorage.getItem(KEY); return saved ? restoreIncident(saved) : null; } catch { return null; } }
function save() { if (state) localStorage.setItem(KEY, serializeIncident(state)); }
function createFact(field, value, evidenceId, sourceReference, confidence = .9, provenanceType = 'evidence') { return { id: id('fact'), field, value: String(value), evidenceId, sourceReference, confidence, provenanceType, userConfirmed: provenanceType === 'user_entered' }; }
async function demoCase() { const bytes = new TextEncoder().encode('CyberSutra synthetic bank receipt: DEMO-UTR-482916 | INR 18500 | 2026-08-25T14:08'); const c = newCase(); c.description = 'I received messages asking me to complete KYC verification and paid a requested fee.'; c.evidence = [{id:'ev_message',type:'WhatsApp screenshot',filename:'whatsapp_kyc_demo.png',mimeType:'image/png',size:182000,source:'Synthetic demo',integrityFingerprint:null,processingStatus:'Synthetic metadata available; extraction unavailable'},{id:'ev_receipt',type:'Bank receipt',filename:'receipt_demo.pdf',mimeType:'application/pdf',size:94000,source:'Synthetic demo fixture',integrityFingerprint:await sha256Hex(bytes),processingStatus:'Synthetic metadata available; extraction unavailable'},{id:'ev_sms',type:'SMS',filename:'sms_demo.txt',mimeType:'text/plain',size:310,source:'Synthetic demo',integrityFingerprint:null,processingStatus:'Synthetic metadata available; extraction unavailable'}]; c.facts = [createFact('phone_number','+91 90000 12345','ev_message','WhatsApp screenshot / contact header',.96),createFact('suspicious_url','https://verify-kyc.example/secure','ev_message','WhatsApp screenshot / message text',.93),createFact('transaction_amount','18500','ev_receipt','Bank receipt / amount',.99),createFact('transaction_timestamp','2026-08-25T14:08','ev_receipt','Bank receipt / timestamp',.98),createFact('transaction_id','DEMO-UTR-482916','ev_receipt','Bank receipt / reference number',.99),createFact('payment_institution','Demo Bank','ev_receipt','Bank receipt / institution',.97),createFact('transaction_amount','15500','ev_sms','SMS / message text',.82)]; c.events = [{id:id('event'),timestamp:'2026-08-25T14:02',description:'Contact initiated a KYC verification conversation.',evidenceIds:['ev_message'],confidence:.92,userConfirmed:false},{id:id('event'),timestamp:'2026-08-25T14:05',description:'A payment was requested through a suspicious link.',evidenceIds:['ev_message'],confidence:.90,userConfirmed:false},{id:id('event'),timestamp:'2026-08-25T14:08',description:'A transaction receipt records a payment.',evidenceIds:['ev_receipt'],confidence:.98,userConfirmed:false}]; return deriveContradictions(c); }
function progress(active) { return `<nav class="progress" aria-label="Case progress">${STEPS.map((label, index) => `<span class="${index === active ? 'current' : index < active ? 'done' : ''}">${index + 1}. ${label}</span>`).join('')}</nav>`; }
function evidenceName(evidenceId) { return state.evidence.find(item => item.id === evidenceId)?.filename || 'User-entered information'; }
function resolutionLabel(conflict) { if (conflict.status === 'unresolved') return 'Not reviewed'; if (conflict.status === 'reviewed_unresolved') return 'Reviewed — unable to verify'; return `Resolved: use ${conflict.resolution?.value} from ${evidenceName(conflict.resolution?.evidenceId)}. Other conflicting value(s) remain preserved as historical evidence.`; }
function render() { const app = $('#app'); if (!state) { app.innerHTML = ''; app.append($('#landing-template').content.cloneNode(true)); bind(); return; } const page = location.hash.slice(1) || 'describe'; const views = {describe,evidence,timeline,review,readiness:readinessView,report,acknowledgement}; app.innerHTML = (views[page] || describe)(); bind(); }
function describe() { return `${progress(0)}<h1 class="page-title">Tell us what happened</h1><p class="subtle">Use your own words. This description is not evidence until you review it.</p><form id="descriptionForm" class="card"><div class="field"><label for="description">What happened?</label><textarea id="description" required maxlength="3000">${html(state.description)}</textarea></div><div class="step-actions"><button class="primary">Save and continue</button></div></form>`; }
function fingerprint(evidence) { return evidence.integrityFingerprint ? `<details class="fingerprint"><summary>File integrity fingerprint · SHA-256 · ${html(shortHash(evidence.integrityFingerprint))}</summary><p>This fingerprint identifies the exact file processed by CyberSutra. It does not establish authenticity or legal admissibility.</p><code>${html(evidence.integrityFingerprint)}</code><button class="text-button" data-copy-hash="${evidence.id}" type="button">Copy full fingerprint</button></details>` : '<p class="subtle">No file fingerprint available.</p>'; }
function evidence() { const selected=state.ui?.selectedEvidenceId; return `${progress(1)}<h1 class="page-title">Add your evidence</h1><p class="subtle">Only file metadata and a local integrity fingerprint are retained in this dependency-free demo. File-content extraction is unavailable. Files are never executed and supplied URLs are never fetched.</p><div class="card upload"><label for="file">Choose PNG, JPEG, PDF or text file (up to 5 MB)</label><input id="file" type="file" accept="image/png,image/jpeg,application/pdf,text/plain" /><p id="uploadError" class="error hidden" role="alert"></p></div><div class="card"><h2>Evidence locker</h2>${state.evidence.length ? state.evidence.map((item,index)=>`<section class="evidence-item ${selected===item.id?'selected':''}" id="evidence-${item.id}"><div class="item"><div><strong>Evidence #${index+1} · ${html(item.filename)}</strong><p>${html(item.type)} · ${Math.round(item.size/1024)} KB · <span class="status">${html(item.processingStatus)}</span></p><p class="subtle">Source: ${html(item.source)}</p>${fingerprint(item)}</div><button class="secondary" data-remove="${item.id}">Remove</button></div></section>`).join('') : '<p class="subtle">No evidence added yet. You can still enter details manually in review.</p>'}</div><div class="step-actions"><a class="secondary" href="#describe">Back</a><a class="primary" href="#timeline">Continue to timeline</a></div>`; }
function timeline() { const events=[...state.events].sort((a,b)=>a.timestamp.localeCompare(b.timestamp)); return `${progress(2)}<h1 class="page-title">Timeline</h1><p class="subtle">Events begin as candidates. They are not confirmed unless you explicitly confirm them.</p><div class="card timeline">${events.length ? events.map(event=>`<article><strong>${new Date(event.timestamp).toLocaleString()}</strong> <span class="status ${event.userConfirmed?'ready':'warning'}">${event.userConfirmed?'User-confirmed event':'Candidate event'}</span><p>${html(event.description)}</p><small>Source: ${event.evidenceIds.map(evidenceName).map(html).join(', ')} · ${Math.round(event.confidence*100)}% confidence</small><p><button class="secondary" data-event-confirm="${event.id}">${event.userConfirmed?'Unconfirm event':'Confirm event'}</button></p></article>`).join('') : '<p class="subtle">No timestamped evidence is available yet.</p>'}</div><div class="step-actions"><a class="secondary" href="#evidence">Back</a><a class="primary" href="#review">Review extracted details</a></div>`; }
function review() { return `${progress(3)}<h1 class="page-title">Review the details</h1><p class="subtle">Evidence-derived values retain their source. User-entered details are clearly labelled and have no invented provenance.</p><div class="card"><table class="facts"><thead><tr><th>Field</th><th>Value</th><th>Source</th><th>Status</th></tr></thead><tbody>${state.facts.map(item=>`<tr><td>${html(item.field.replaceAll('_',' '))}</td><td>${html(item.value)}</td><td>${isManualFact(item)?'<span class="status">USER-ENTERED</span>':`<button class="source" data-source="${item.evidenceId}">${html(item.sourceReference)}</button>`}</td><td>${isManualFact(item)?'User-entered':`<label><input type="checkbox" data-fact-confirm="${item.id}" ${item.userConfirmed?'checked':''}/> Confirm</label>`}</td></tr>`).join('')}</tbody></table></div><div class="card"><h2>Add a detail manually</h2><form id="factForm" class="grid"><div class="field"><label>Field <select id="factField"><option value="transaction_amount">Transaction amount</option><option value="transaction_id">Transaction ID</option><option value="transaction_timestamp">Transaction time</option><option value="payment_institution">Payment institution</option><option value="phone_number">Phone number</option></select></label></div><div class="field"><label>Value <input id="factValue" maxlength="160" required /></label></div><div class="field"><button class="secondary">Add detail</button></div></form></div><div class="step-actions"><a class="secondary" href="#timeline">Back</a><a class="primary" href="#readiness">Check readiness</a></div>`; }
function readinessView() { const readiness=calculateReadiness(state); return `${progress(4)}<h1 class="page-title">Report readiness</h1><div class="card"><span class="status ${readiness.state==='READY'?'ready':readiness.state==='INCOMPLETE'?'danger':'warning'}">${readiness.state}</span><h2>${readiness.state==='READY'?'Ready for mock submission':readiness.state==='INCOMPLETE'?'More critical information is needed':'Review required before submission'}</h2>${readiness.missing.length?`<div class="error"><strong>Missing critical information:</strong> ${readiness.missing.map(field=>html(field.replaceAll('_',' '))).join(', ')}.</div>`:''}${readiness.criticalOpen?'<div class="error"><strong>Critical contradictions require an explicit resolution.</strong> CyberSutra will not choose a conflicting value for you.</div>':''}${readiness.unconfirmedRequired?'<div class="callout"><strong>Required evidence-derived values still need confirmation.</strong></div>':''}<p class="subtle">This state is calculated by explicit rules, not an acceptance prediction.</p></div>${state.contradictions.length?`<div class="card"><h2>Contradictions</h2>${state.contradictions.map(conflict=>`<section class="contradiction"><h3>${html(conflict.field.replaceAll('_',' '))} mismatch <span class="status ${conflict.status==='resolved'?'ready':'warning'}">${html(conflict.status.replaceAll('_',' '))}</span></h3><p>${html(resolutionLabel(conflict))}</p><form data-conflict-form="${conflict.id}">${conflict.factIds.map(factId=>{const fact=state.facts.find(item=>item.id===factId);return `<label class="choice"><input type="radio" name="${conflict.id}" value="${fact.id}" ${conflict.resolution?.chosenFactId===fact.id?'checked':''}/> Use ${html(fact.value)} from ${html(evidenceName(fact.evidenceId))}</label>`;}).join('')}<label class="choice"><input type="radio" name="${conflict.id}" value="unresolved" ${conflict.status==='reviewed_unresolved'?'checked':''}/> Mark as unresolved / unable to verify</label><button class="secondary">Save explicit resolution</button></form></section>`).join('')}</div>`:''}<div class="step-actions"><a class="secondary" href="#review">Back</a><a class="primary" href="#report">Review report</a></div>`; }
function report() { const readiness=calculateReadiness(state); return `${progress(5)}<h1 class="page-title">Report review</h1><div class="notice"><strong>Mock report only.</strong> This does not contact NCRP or any government system.</div><article class="card"><h2>Financial cyber fraud — draft</h2><p>${html(state.description||'No incident description provided.')}</p><h3>Evidence-linked details</h3>${state.facts.length?`<ul>${state.facts.map(item=>`<li>${html(item.field.replaceAll('_',' '))}: ${html(item.value)} <small>(${isManualFact(item)?'USER-ENTERED':`source: ${html(item.sourceReference)}`})</small></li>`).join('')}</ul>`:'<p>No details added.</p>'}<h3>Contradiction resolutions</h3>${state.contradictions.length?`<ul>${state.contradictions.map(conflict=>`<li>${html(conflict.field.replaceAll('_',' '))}: ${html(resolutionLabel(conflict))}</li>`).join('')}</ul>`:'<p>None detected.</p>'}<h3>Readiness</h3><p><span class="status ${readiness.state==='READY'?'ready':'warning'}">${readiness.state}</span></p></article><div class="step-actions"><a class="secondary" href="#readiness">Back</a><button class="primary" data-action="submit" ${readiness.canSubmit?'':'disabled'}>Submit mock report</button>${readiness.canSubmit?'':'<p class="subtle">Mock submission is available only when the deterministic readiness state is READY.</p>'}</div>`; }
function acknowledgement() { return `${progress(6)}<h1 class="page-title">Mock acknowledgement</h1><div class="success"><h2>Mock report recorded</h2><p>Your local demo acknowledgement is <strong>${html(state.acknowledgement||'not available')}</strong>.</p><p>No data was sent outside this browser. This acknowledgement is not valid for a real complaint.</p></div><div class="step-actions"><button class="primary" data-action="new-case">Start another incident</button></div>`; }
function bind() { const descriptionForm=$('#descriptionForm'); if(descriptionForm)descriptionForm.onsubmit=event=>{event.preventDefault();state.description=$('#description').value.trim();save();location.hash='evidence';}; const file=$('#file');if(file)file.onchange=handleUpload; document.querySelectorAll('[data-remove]').forEach(button=>button.onclick=()=>{const evidenceId=button.dataset.remove;state.evidence=state.evidence.filter(item=>item.id!==evidenceId);state.facts=state.facts.filter(item=>item.evidenceId!==evidenceId);deriveContradictions(state);save();render();});document.querySelectorAll('[data-source]').forEach(button=>button.onclick=()=>{state.ui.selectedEvidenceId=button.dataset.source;save();location.hash='evidence';render();});document.querySelectorAll('[data-copy-hash]').forEach(button=>button.onclick=async()=>{const hash=state.evidence.find(item=>item.id===button.dataset.copyHash)?.integrityFingerprint;try{await navigator.clipboard.writeText(hash);button.textContent='Copied';}catch{button.textContent='Copy unavailable';}});document.querySelectorAll('[data-event-confirm]').forEach(button=>button.onclick=()=>{const event=state.events.find(item=>item.id===button.dataset.eventConfirm);event.userConfirmed=!event.userConfirmed;save();render();});document.querySelectorAll('[data-fact-confirm]').forEach(control=>control.onchange=()=>{state.facts.find(item=>item.id===control.dataset.factConfirm).userConfirmed=control.checked;save();});const factForm=$('#factForm');if(factForm)factForm.onsubmit=event=>{event.preventDefault();state.facts.push(createFact($('#factField').value,$('#factValue').value.trim(),null,null,1,'user_entered'));deriveContradictions(state);save();render();};document.querySelectorAll('[data-conflict-form]').forEach(form=>form.onsubmit=event=>{event.preventDefault();const choice=new FormData(form).get(form.dataset.conflictForm);if(!choice)return;setContradictionResolution(state,form.dataset.conflictForm,choice);save();render();});document.querySelectorAll('[data-action]').forEach(button=>button.onclick=async()=>{if(button.dataset.action==='new-case'){state=newCase();save();location.hash='describe';}if(button.dataset.action==='load-demo'){state=await demoCase();save();location.hash='evidence';}if(button.dataset.action==='submit'&&calculateReadiness(state).canSubmit){state.submitted=true;state.acknowledgement=`MOCK-${new Date().getFullYear()}-${Math.random().toString(36).slice(2,8).toUpperCase()}`;save();location.hash='acknowledgement';}}); }
async function handleUpload(event) { const file=event.target.files[0];const error=$('#uploadError');if(!file)return;const result=validateUpload(file);if(!result.ok){error.textContent=result.reason;error.classList.remove('hidden');event.target.value='';return;}try{const integrityFingerprint=await sha256Hex(await file.arrayBuffer());state.evidence.push({id:id('ev'),type:file.type==='application/pdf'?'Document':file.type==='text/plain'?'Text message':'Screenshot',filename:sanitizeFilename(file.name),mimeType:file.type,size:file.size,source:'Uploaded by citizen',integrityFingerprint,processingStatus:'Metadata retained locally; extraction unavailable'});save();render();}catch(problem){error.textContent=`The file was not stored: ${problem.message}`;error.classList.remove('hidden');} }
window.addEventListener('hashchange',render);$('#resetCase').onclick=()=>{localStorage.removeItem(KEY);state=null;location.hash='start';render();};render();
